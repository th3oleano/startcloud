server {
    listen 80;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;

    location / {
        try_files $uri $uri/ =404;
    }

    # Não faz nenhum redirecionamento de domínio ou protocolo
    # Não força HTTPS
}
